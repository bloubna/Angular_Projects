import { MyStyleClassDirective } from './my-style-class.directive';

describe('MyStyleClassDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStyleClassDirective();
    expect(directive).toBeTruthy();
  });
});
